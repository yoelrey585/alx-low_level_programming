#include <stdio.h>

/**
 * main - Prints first 10 whole numbers
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int z;

	for (z = 48; z < 58; z++)
	{
		putchar(z);
	}
	putchar('\n');
	return (0);
}
