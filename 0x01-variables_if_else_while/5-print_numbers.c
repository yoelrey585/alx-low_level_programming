#include <stdio.h>

/**
 * main - Prints the first nine whole numbers
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int z;

	for (z = 0; z < 10; z++)
	{
		printf("%d", z);
	}
	putchar('\n');
	return (0);
}
